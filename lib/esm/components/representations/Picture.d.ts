import { PictureProps } from "./types";
/**
 * Picture is single picture display with pan effect.
 */
declare const Picture: (props: PictureProps) => import("react/jsx-runtime").JSX.Element;
export default Picture;
